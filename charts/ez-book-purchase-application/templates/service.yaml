apiVersion: v1
kind: Service
metadata:
  name: {{ include "ez-book-purchase-application.fullname" . }}
  labels:
    {{- include "ez-book-purchase-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ez-book-purchase-application.selectorLabels" . | nindent 4 }}
